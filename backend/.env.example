NODE_ENV=development
PORT=3000

# Neon dev branch — pooled connection (app runtime via PrismaPg adapter)
# Note the "-pooler" in the hostname
DATABASE_URL=postgresql://user:password@ep-cool-dawn-123456-pooler.us-east-2.aws.neon.tech/dbname?sslmode=require

# Neon dev branch — direct connection (migrations only)
# Same hostname WITHOUT "-pooler"
DATABASE_URL_UNPOOLED=postgresql://user:password@ep-cool-dawn-123456.us-east-2.aws.neon.tech/dbname?sslmode=require

JWT_SECRET=change-me-to-a-random-secret-at-least-32-chars
ADMIN_DEFAULT_USERNAME=admin@agentguild.tech
ADMIN_DEFAULT_PASS=
